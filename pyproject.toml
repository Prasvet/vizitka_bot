[project]
name = "vizitka-bot"
version = "0.1.0"
description = "Telegram bot for business cards"
readme = "README.md"
requires-python = ">=3.12,<3.14"

dependencies = [
  "aiogram>=3.0.0",
  "python-dotenv",
]
